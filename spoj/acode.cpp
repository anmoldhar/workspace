#include<iostream>
using namespace std;
long long numofdeco(int *enc)
int main()
{
  char inp[5000] = {0};
  int enc[5000];
  cin >> inp;
  while(*inp != '0')
  {
    fill_n(enc, 5000, -1)
    for(int i =0; inp!= '\0'; i++)\
    {
      enc[i] = inp[i] - '0';
      inp[i] = 0;
    }
    cout << numofdeco(enc);
    cin >> inp;
  }
}

long long numofdeco(int * a)
{
  if(*a == -1 || *(a+1)== -1)
    return
}
